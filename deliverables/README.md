# SignalScope deliverables

Local artifacts generated during Phase 7:

| Artifact | Purpose |
|---|---|
| `SignalScope-1.0.0-preview.apk` | Direct-install Android preview for `arm64-v8a` and `x86_64` |
| `SignalScope-demo.mp4` | 69-second emulator walkthrough |

APK SHA-256:

```text
17180921ffb72baba5731a85ed770760f69f4298f1f838b835f606b3a4f81312  SignalScope-1.0.0-preview.apk
```

The binary artifacts are excluded from Git history. Upload them to a GitHub Release or the assignment's Drive folder for delivery.
